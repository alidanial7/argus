server {
    listen 80;
    listen [::]:80;
    server_name ${DOMAIN_WEBUI};

    location / {
        proxy_pass http://open-webui:8080;
        include /etc/nginx/snippets/proxy-params.conf;
    }
}
