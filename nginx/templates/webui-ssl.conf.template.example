# Optional HTTPS for Open WebUI
# 1. Place certs in data/nginx/certs/ (fullchain.pem + privkey.pem) or run scripts/generate-certs.sh
# 2. Rename this file to webui-ssl.conf.template (remove .example) to enable
#
# server {
#     listen 443 ssl;
#     listen [::]:443 ssl;
#     http2 on;
#     server_name ${DOMAIN_WEBUI};
#
#     ssl_certificate     /etc/nginx/certs/fullchain.pem;
#     ssl_certificate_key /etc/nginx/certs/privkey.pem;
#     include /etc/nginx/snippets/ssl-params.conf;
#
#     location / {
#         proxy_pass http://open-webui:8080;
#         include /etc/nginx/snippets/proxy-params.conf;
#     }
# }
#
# server {
#     listen 80;
#     listen [::]:80;
#     server_name ${DOMAIN_WEBUI};
#     return 301 https://$host$request_uri;
# }
