server {
    listen 80;
    listen [::]:80;
    server_name ${DOMAIN_SEARXNG};

    location / {
        proxy_pass http://searxng:8080;
        include /etc/nginx/snippets/proxy-params.conf;
    }
}
